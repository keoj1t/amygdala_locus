"use client";
import React from "react";
import { Building2, FlaskConical, Layers3, MapPin, Trophy, Users, BedDouble } from "lucide-react";
import { CategoryType } from "@/types/campus";
const tabs: { id: CategoryType | "all"; label: string; icon: React.ElementType }[] = [{id:"all",label:"Все",icon:Layers3},{id:"campus",label:"Кампус",icon:Building2},{id:"dorm",label:"Общежития",icon:BedDouble},{id:"lab",label:"Лаборатории",icon:FlaskConical},{id:"sport",label:"Спорт",icon:Trophy},{id:"city",label:"Город",icon:MapPin},{id:"student_life",label:"Жизнь",icon:Users}];
export const FilterBar = ({ activeCategory, onSelectCategory, categoryCounts }: { activeCategory: CategoryType | "all"; onSelectCategory: (cat: CategoryType | "all") => void; categoryCounts: Record<string, number>; trustFilter: "all" | "verified_only" | "needs_check_only"; onSelectTrustFilter: (tf: "all" | "verified_only" | "needs_check_only") => void; sortBy: "trust" | "newest"; onSelectSortBy: (sort: "trust" | "newest") => void }) => <div className="category-bar shell"><p>Фотографии кампуса</p><div>{tabs.map((tab) => { const Icon=tab.icon; const count=tab.id === "all" ? Object.values(categoryCounts).reduce((a,b)=>a+b,0) : categoryCounts[tab.id] || 0; return <button key={tab.id} onClick={() => onSelectCategory(tab.id)} className={activeCategory === tab.id ? "active" : ""}><Icon size={15}/>{tab.label}<small>{count}</small></button> })}</div></div>;
